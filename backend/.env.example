DB_HOST=localhost
DB_NAME=snippetmate
DB_PASSWORD=your_password_here
DB_USER=snippetmate_user
# Path to the Firebase service-account JSON file (outside the repo, never commit the file itself)
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/firebase-service-account.json
PORT=3000
